# TODO

- add reconstruction export or grid rendering for the MNIST autoencoder
- try a denoising or variational autoencoder variant after the plain baseline
- build a small attention-based seq2seq demo
- compare skip-gram initialized embeddings against the current n-gram baseline
- improve sampling in `ngram.c` beyond greedy decoding
- AWS training script. various machines

